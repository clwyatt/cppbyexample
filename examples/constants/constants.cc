// A C++ program has an entry point
// where the code starts running called
// main.

// main is a function that returns an integer
// more about functions and other versions
// of main later
int main()
{
  // return a code to the invoking shell
  return 0;
}
